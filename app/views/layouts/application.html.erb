<!DOCTYPE html>
<html lang="en">
<head>
  <title>猎了么-在线有偿职位分享</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  
  <!-- ie8 和ie9s support-->
  <script src="/respondjs/html5shiv.js"></script>
  <script src="/respondjs/respond.min.js"></script>
      
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Lieyohui" %></title>



    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
</head>
<body  class='<%= controller.controller_name %>'>
  <!--div of modal,trigger link is put to navbar-->	
  <%= render 'layouts/search_tips' %>

  <!--navbar-inverse设置bar的颜色是黑色-->
  <nav class="navbar navbar-color navbar-fixed-top">
  <%= render 'layouts/logo.html.erb' %> 
      <div class="container">
        <div class="navbar-header">       
            <%= render 'layouts/search_form' %>
        </div>

	<div id="navbar" class="navbar-collapse collapse">
	  <% if user_signed_in? %>
	    <% case current_user.user_type %>
	    <% when "求职者" %>
	      <%= render 'layouts/talent' %>
    	    <% when "猎头" %>
    	      <%= render 'layouts/hunter' %>
            <% when "人事" %>
	      <%= render 'layouts/hr' %>
	    <% end %>
    	  <% else %><!--没有登录-->
            <%= render 'layouts/signin_form' %>
          <% end %> 
        </div><!--/.navbar-collapse -->
      </div><!-- container-->
  </nav>

<div id="content"><%= content_for?(:content) ? yield(:content) : yield %></div>
<div class='row'>
</div>
<% cache('footer') do %>
	<%= render 'layouts/footer' %>
<% end %>
</body>
</html>
